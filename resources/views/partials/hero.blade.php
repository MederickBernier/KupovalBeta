<section class="row justify-content-center hero">
    <div class="col-md-8 text-center">
        <h1 class="display-4">{{ __('hero.welcome', ['app_name' => config('app.name')]) }}</h1>
        <p class="lead">{{ __('hero.description') }}</p>
    </div>
</section>
